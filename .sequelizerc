const path = require('path');

module.exports = {
    config: path.resolve(__dirname, 'app', 'config', 'database-config.js'),
    'migrations-path': path.resolve(__dirname, 'app', 'database', 'migrations'),
};